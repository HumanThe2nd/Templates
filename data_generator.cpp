#include <iostream>
#include <random>
int main(int argc, char** argv) {
    const int suite=atoi(argv[1],seed=atoi(argv[2]); // choose seed based on case number
    std::mt19937 gen(seed);  // Use the provided seed
    std::uniform_int_distribution<> dis(1,100); // generator between 1 to 100 inclusive
    int k=dis(gen),n=dis(gen),l=dis(gen),m=0;
    if(suite==1){ // batch 1
        // batch 1 body
        return 0;
    }
    if(suite==2){ // batch 2
        // batch 2 body
       return 0;
    }
    // other batches
}
